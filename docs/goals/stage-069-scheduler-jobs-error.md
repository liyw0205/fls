# 阶段 69：调度任务查询异常回归

## 目标

固定 `/api/scheduler/jobs` 在调度器查询失败时的错误响应，避免状态页轮询收到 HTML
错误页或缺失字段。

## 范围

- 在现有 API 测试中 mock 调度器异常。
- 验证 HTTP 状态、`ok`、`msg` 和 `jobs` 字段。
- 不改变正常查询路径。

## 验收

- 查询异常返回 `500` JSON，且 `jobs` 为列表。
- 全量测试、响应式结构烟测、编译检查与 diff 检查通过。
