# 阶段 94：依赖与运行时安装隔离验证

为依赖安装和运行时安装入口补充请求级 mock 回归，固定安装记录、日志跳转和命令边界。

验收：

- POST `/deps/install` 注册 `DEPS_RUNNING` 并跳转安装日志页，测试不启动真实 pip 进程。
- GET `/install/runtime/<runtime>` 注册运行时安装记录并跳转日志页，测试不调用系统包管理器。
- 进程命令、工作目录和日志路径可被断言，测试使用隔离 `FLS_BASE_DIR`。
- 目标测试、全量单元测试、响应式烟测、编译检查和差异检查通过。
