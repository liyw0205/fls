# 阶段 97：面板重启与停止隔离验证

为关于页的面板重启/停止 POST 增加 mock 控制线程回归，固定用户可见状态和进程边界。

验收：

- POST `/about/restart-panel` 渲染“正在重启面板”状态并创建 mock 线程，不执行控制脚本或终止当前进程。
- POST `/about/stop-panel` 渲染“正在停止面板”状态并创建 mock 线程，不执行控制脚本或终止当前进程。
- 控制脚本路径使用隔离临时文件，目标测试、全量单元测试、响应式烟测、编译检查和差异检查通过。
