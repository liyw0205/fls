# 阶段 66：批量运行失败明细回归

## 目标

固定批量运行任务在部分提交失败时的结构化返回和错误摘要上限，避免前端无法判定
成功数、失败数或展示过长错误内容。

## 范围

- 在 `tests/test_bulk_workflows.py` 覆盖混合成功、失败的批量运行请求。
- 验证 `submitted_count`、`failed_count`、`failures` 与消息摘要。
- 不触发真实任务进程。

## 验收

- 失败明细保留任务名和业务错误，摘要最多显示前三条并带总数提示。
- 全量测试、响应式结构烟测、编译检查与 diff 检查通过。
