# 阶段 92：任务与日志分页组件收束

将任务和日志页面的重复分页标记收束到共享 `pagination_card()`，并让非法页码安全回到第一页。

验收：

- `/tasks` 和 `/logs` 均通过 `pagination_card()` 渲染分页，分别保留任务页的 `q/sort` 与日志页的 `q` 查询参数。
- 两页收到非法 `page` 参数时返回 `200`，展示第 1 页，不抛出 `ValueError` 或 HTML 500。
- 共享组件、真实路由和全量测试通过。
