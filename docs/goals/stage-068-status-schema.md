# 阶段 68：运行状态接口 Schema 回归

## 目标

为 `/api/status` 固定面板轮询使用的任务状态字段，避免任务运行态或命令名称变化时
破坏前端渲染。

## 范围

- 在现有 API 测试中覆盖运行中和未运行任务的状态响应。
- 验证任务标识、启用状态、运行计数、PID 和进程名称字段。
- 使用 mock 隔离实际进程状态。

## 验收

- 接口始终为每个任务返回稳定字段，未运行任务的 PID 为 `null`。
- 全量测试、响应式结构烟测、编译检查与 diff 检查通过。
